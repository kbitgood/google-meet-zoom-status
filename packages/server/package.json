{
  "name": "@gmzs/server",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run index.ts",
    "compile": "bun build index.ts --compile --outfile ../../bin/zoom-automator --external electron --external 'chromium-bidi/*'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.51.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.5"
  }
}
